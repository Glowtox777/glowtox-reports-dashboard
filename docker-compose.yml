services:
  dashboard:
    build: .
    container_name: glowtox-reports-dashboard
    command: npm start
    expose:
      - "4173"
    environment:
      USE_REAL_REPORT_DATA: "false"
    restart: unless-stopped
